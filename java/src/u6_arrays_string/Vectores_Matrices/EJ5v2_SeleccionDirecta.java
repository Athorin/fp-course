/**
 * Seleccion Directa de Ordenacion de Vectores:
 * 
 * Realiza N-1 pasadas sobre el vector a ordenar, siendo N el tamaño del mismo.
 * En la primera pasada busca el elemento de menor valor y los coloca
 * en la primera posicion (con un intercambio);
 * En la segunda pasada busca, entre los elementos restantes, de nuevo el
 * menor y lo coloca en la segunda posicion. Y asi sucesivamente.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class EJ5v2_SeleccionDirecta
{
   public static void main()
   {
       int vector [] = {2,4,5,3,1};
       
       int i,j;
       
       for (i = 0; i < vector.length; i++)
       {
           for (j = 0; j < vector.length-1; j++)
           {
              dfa
           }
       }
   }
}
